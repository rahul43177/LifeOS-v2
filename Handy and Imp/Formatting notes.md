Please format the file located at:
```plain text
/Users/rahulmishra/Library/Mobile Documents/iCloud~md~obsidian/Documents/LifeOS/Work/RL EU/Second Half - Listing new table.md
```

Follow these instructions carefully:

1. **Preserve Obsidian Syntax:** Do NOT modify any Obsidian-specific syntax including:
   - Internal links: `[[file-name]]` or `[[file-name|display-text]]`
   - Highlights: `==highlighted text==`
   - Tags: `#tag-name`
   - Math expressions: `$$ formula $$`
   - Code block metadata: `fold title:filename.ext`

2. **Remove Extra Whitespace:**
   - Remove multiple consecutive blank lines (max 2 blank lines between sections)
   - Remove trailing whitespace at end of lines
   - Remove excessive spaces within lines (preserve single spaces)
   - Keep one blank line between paragraphs
   - Keep one blank line before/after headings, code blocks, and lists

3. **Format Code Blocks:**
   - Identify all fenced code blocks (```)
   - Determine the correct language identifier (js, javascript, sql, html, bash, etc.)
   - Apply language-specific indentation and formatting
   - Preserve any `fold title:` syntax in code block headers
   - Ensure consistent 2 or 4 space indentation within code blocks

4. **Fix List Formatting:**
   - Ensure consistent indentation for nested lists (2 spaces per level)
   - Fix broken list continuations
   - Preserve numbered vs. bulleted list types

5. **Preserve All Content:** Do not change, add, or delete any actual text, information, code logic, or content. Only format for readability.

In summary: Clean up whitespace and formatting while preserving all Obsidian syntax and content integrity.